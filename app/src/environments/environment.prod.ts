/**
 * Environment - Produção
 * Usado quando a aplicação é deployada
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.priceoff.com' // Altere para URL real da sua API em produção
};
